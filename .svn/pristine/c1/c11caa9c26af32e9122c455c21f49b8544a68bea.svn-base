using System;
using Microsoft.Practices.Prism.Modularity;
using Microsoft.Practices.ServiceLocation;
using Microsoft.Practices.Unity;

namespace Module.SQL
{
    internal class SqlModule: IModule
    {
        public void Initialize()
        {
            Console.WriteLine($@"{nameof(SqlModule)} has been initialized at{DateTime.Now}.");

            //ServiceLocator.Current.GetInstance<IUnityContainer>().RegisterInstance()
        }    
    }
}
